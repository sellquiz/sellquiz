%course Beispielkurs
%quiz 1: Beispielquiz


Deklarationen

Schreiben Sie einen Codeausschnitt, in dem zwei Variablen wie folgt definiert werden:
* Eine Variable mit dem Namen "x" speichert eine Gleitkommazahl und weist dieser den Wert 1.23 zu.
* Eine weitere Variable mit dem Namen "text" soll die Zeichenkette "hallo" beinhalten.
_Hinweis: schreiben Sie nur die Deklarationen selbst: Geben Sie keine Klasse und keinen Methodenkopf an!_

	JavaBlock b
		assert 'Math.abs(x-1.23) < 1e-3'
		assert 'text.equals("hallo")'
    x in { 1, 2, 3 }   % TODO: remove this line!

$ #b $


%%%


Methoden

	a, b in { 3, 4, ..., 10 }
	c := a + b
	JavaMethod add
		% assert 'add($a, $b) == $c'   TODO!!!!!
        assert 'add(2, 3) == 5'

Definieren Sie eine Java-Methode mit dem Namen __add__.
* Die Methode hat zwei ganzzahlige Parameter.
* Der Rückgabetyp soll ebenfslls ganzzahlig sein.
* Die Methode gibt die Summe der beiden Parameterwerte zurück.

_Hinweis: definieren Sie nur die Methode selbst. Vergessen Sie nicht die Methode als öffentlich und statisch zu deklarieren. Geben Sie keine Klasse an._

$ #add $
